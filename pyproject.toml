[project]
name = "db-magic"
version = "0.1.0"
description = "Database sandbox tryout code."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.12.5",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.15.9",
]
